# Dataset001_XXY — nnUNet 项目依赖
#
# 推荐环境：Python 3.10–3.14（当前在 3.14 + CUDA 12.6 下验证）
#
# 安装步骤（Windows + NVIDIA GPU 示例）：
#   1. conda create -n nnunet python=3.11 -y
#   2. conda activate nnunet
#   3. 先安装 PyTorch（按显卡/CUDA 版本选择，见下方注释）
#   4. pip install -r requirements.txt
#   5. pip install -e .
#
# 环境变量（训练/推理前设置）：
#   nnUNet_raw          原始数据目录
#   nnUNet_preprocessed 预处理输出目录
#   nnUNet_results      模型与推理结果目录
#
# ---------------------------------------------------------------------------
# PyTorch — 请单独安装，不要写进下方列表（CUDA 版本因机器而异）
# ---------------------------------------------------------------------------
# CUDA 12.6（RTX 40 系列等）：
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# CPU 版：
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# ---------------------------------------------------------------------------

# --- nnUNet v2 核心（与 pyproject.toml 一致）---
acvl-utils>=0.2.6,<0.3
dynamic-network-architectures>=0.4.4,<0.5
batchgenerators>=0.25.1
batchgeneratorsv2>=0.3.2
blosc2>=3.3.2
connected-components-3d>=3.0
einops>=0.8
tqdm>=4.60
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
scikit-image>=0.19.3
SimpleITK>=2.2.1
pandas>=2.0
nibabel>=5.0
matplotlib>=3.7
seaborn>=0.13
graphviz>=0.20
tifffile>=2023.1
imagecodecs>=2023.1
yacs>=0.1.8
requests>=2.28
PyYAML>=6.0

# --- 本项目 scripts/ 数据分析脚本额外使用 ---
# nibabel / scipy / scikit-image 已包含在上文

# --- 可选：LightGBM 子项目（LightGBM/ 目录）---
# pip install -r LightGBM/requirements.txt
